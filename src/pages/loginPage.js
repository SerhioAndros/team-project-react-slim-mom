import React from "react";
import LoginForm from "../components/loginForm/LoginForm";

const loginPage = () => {
  return (
    <div>
      <h2>ВХОД</h2>
      <LoginForm />
    </div>
  );
};

export default loginPage;
